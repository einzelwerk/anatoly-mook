import Image from 'next/image';
import React, { useState } from 'react';
import { Gallery, Item } from 'react-photoswipe-gallery';

import 'photoswipe/dist/photoswipe.css';

export const PopupVideo = ({ id, data }) => {
    return <Gallery></Gallery>;
};
